'use strict';

/* jasmine specs for services go here */

describe('Sudoku services', function() {

  describe('sudokuFactory', function() {

    var sudokuFactory = {};

    xit('should change the game level', function() {
      expect(sudokuFactory.processLevel("medium")).toEqual(40);
    })

  });

});
